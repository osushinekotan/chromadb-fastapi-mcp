# ChromaDB settings
CHROMA_CLIENT_TYPE=ephemeral   # Options: ephemeral, persistent
CHROMA_DATA_DIR=./chroma_data  # Required for persistent client
CHROMA_DOTENV_PATH=.env        # Path to .env file

# OpenAI API key for embedding function
OPENAI_API_KEY=your_openai_api_key_here  # Required for OpenAI embedding function

# Application settings
APP_HOST=0.0.0.0
APP_PORT=8000